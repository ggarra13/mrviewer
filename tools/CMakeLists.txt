

ADD_SUBDIRECTORY( ctlparser )

IF(WIN32 OR WIN64 OR CYGWIN OR MINGW)
ELSE(WIN32 OR WIN64 OR CYGWIN OR MINGW)
ADD_SUBDIRECTORY( dvd )
ENDIF(WIN32 OR WIN64 OR CYGWIN OR MINGW)

ADD_SUBDIRECTORY( filereq )
